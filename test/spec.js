const {expect} = require('chai')

describe('User.byToken', ()=>{
        describe('with a valid token', ()=> {
            it('returns a user', async()=> {
                expect(1).to.equal(1)
            })
        })
    })